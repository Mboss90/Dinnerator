import React from 'react';

// Does header need to be individual component? Review and update.
// Testing sync issue with Github.

const Header = (props) => {
  return (
    <div>
      <h1 className="textHeader">Dinnerator</h1>
    </div>
  );
}

export default Header;